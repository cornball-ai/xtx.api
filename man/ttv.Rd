% Generated by rhydrogen: do not edit by hand
% Please edit documentation in R/ttv.R
\name{ttv}
\alias{ttv}
\title{Generate Video from Text (Text-to-Video)}
\usage{
ttv(
  prompt,
  output = "ttv_output.mp4",
  backend = "wan2gp",
  model = "ltx2",
  width = 832L,
  height = 832L,
  num_frames = 129L,
  num_steps = NULL,
  guidance_scale = NULL,
  seed = NULL,
  timeout = 600
)
}
\arguments{
\item{prompt}{Text prompt describing the video content}

\item{output}{Path for output video file (default: "ttv_output.mp4")}

\item{backend}{Backend to use: "wan2gp" (default)}

\item{model}{Model ID: "ltx2" (default)}

\item{width}{Video width in pixels (default: 832)}

\item{height}{Video height in pixels (default: 832)}

\item{num_frames}{Number of frames to generate (default: 129, ~5.4s at 24fps)}

\item{num_steps}{Number of inference steps (NULL = model default)}

\item{guidance_scale}{CFG scale (NULL = model default)}

\item{seed}{Random seed for reproducibility}

\item{timeout}{Request timeout in seconds (default: 600)}
}
\value{
Invisibly returns the output file path
}
\description{
Generate a video from a text prompt alone (no input image).
Uses LTX-2 via WanGP.
}
\examples{
\dontrun{
  # Simple text-to-video
  ttv("A cat playing piano in a jazz club", output = "cat_piano.mp4")

  # With custom parameters
  ttv("Aerial view of mountains at sunset",
      width = 1280, height = 720, num_frames = 161,
      seed = 42, output = "mountains.mp4")
}
}
