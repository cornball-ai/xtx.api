% tinyrox says don't edit this manually, but it can't stop you!
\name{.stv_diffuseR}
\alias{.stv_diffuseR}
\title{stv() via diffuseR: start image + audio -> audio-driven talking head}
\usage{
.stv_diffuseR(
  image,
  audio,
  output,
  prompt,
  resolution,
  quality,
  seed = NULL,
  timeout = NULL
)
}
\description{
stv() via diffuseR: start image + audio -> audio-driven talking head
}
\keyword{internal}
