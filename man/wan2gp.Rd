% Generated by rhydrogen: do not edit by hand
% Please edit documentation in R/wan2gp.R
\name{wan2gp}
\alias{wan2gp}
\title{Generate Video with WanGP (LTX-2)}
\usage{
wan2gp(
  prompt,
  image = NULL,
  audio = NULL,
  output = "wan2gp_output.mp4",
  width = 832L,
  height = 832L,
  num_frames = 129L,
  steps = 8L,
  guidance_scale = 4,
  seed = NULL,
  model = c("distilled", "dev"),
  timeout = 600
)
}
\arguments{
\item{prompt}{Text prompt describing the video content}

\item{image}{Path to starting image (optional, for image-to-video)}

\item{audio}{Path to audio file for lip-sync/audio-guided generation (optional}

\item{output}{Path for output video file}

\item{width}{Video width in pixels (default: 832)}

\item{height}{Video height in pixels (default: 832)}

\item{num_frames}{Number of frames to generate (default: 129, ~5.4s at 24fps)}

\item{steps}{Number of inference steps (default: 8 for distilled model)}

\item{guidance_scale}{CFG scale (default: 4.0)}

\item{seed}{Random seed for reproducibility (default: random)}

\item{model}{Model variant: "distilled" or "dev" (default: "distilled")}

\item{timeout}{Maximum time to wait in seconds (default: 600)}
}
\value{
Invisibly returns the output file path
}
\description{
Generate video using LTX-2 19B via WanGP Docker container. Supports
text-to-video, image-to-video, and audio-guided generation.
}
\examples{
\dontrun{
  # Text-to-video
  wan2gp("A cat playing piano", output = "cat_piano.mp4")

  # Image-to-video
  wan2gp("Person talking and gesturing",
         image = "portrait.jpg",
         output = "talking.mp4")

  # Audio-guided video
  wan2gp("Person speaking into microphone",
         image = "dj.jpg",
         audio = "speech.wav",
         output = "dj_speaking.mp4")
}
}
