% Generated by rhydrogen: do not edit by hand
% Please edit documentation in R/ip_adapter.R
\name{ip_adapter}
\alias{ip_adapter}
\title{Generate Image with IP-Adapter}
\usage{
ip_adapter(
  prompt,
  reference_image,
  ip_adapter_type = c("faceid", "faceid-plus", "face", "plus", "standard"),
  model = "stabilityai/stable-diffusion-xl-base-1.0",
  ip_adapter_scale = 0.6,
  negative_prompt = "lowres, bad anatomy, worst quality, low quality",
  steps = 30,
  guidance_scale = 7.5,
  width = 1024,
  height = 1024,
  file = NULL,
  timeout = 300
)
}
\arguments{
\item{prompt}{Character. The text description of the image to generate.}

\item{reference_image}{Character. Path to reference image file OR base64 string.}

\item{ip_adapter_type}{Character. Type of IP-Adapter to use:
- "standard": Basic style/content transfer
- "plus": Better detail preservation (ViT-H encoder)
- "face": Face generation using CLIP (good for faces)
- "faceid": Uses InsightFace embeddings (best for face accuracy)
- "faceid-plus": Combines FaceID + CLIP (best quality faces)}

\item{model}{Character. Base model to use. Default "stabilityai/stable-diffusion-xl-base-1.0".}

\item{ip_adapter_scale}{Numeric. Influence of reference image (0-1). Default 0.6.}

\item{negative_prompt}{Character. What to avoid in the image.}

\item{steps}{Integer. Number of inference steps. Default 30.}

\item{guidance_scale}{Numeric. CFG scale. Default 7.5.}

\item{width}{Integer. Output image width. Default 1024.}

\item{height}{Integer. Output image height. Default 1024.}

\item{file}{Character or NULL. If provided, save the image to this path.}

\item{timeout}{Numeric. Request timeout in seconds. Default 300.}
}
\value{
A list with the generated image data, or invisibly returns the file path
  if `file` is specified.
}
\description{
Generate an image using IP-Adapter with a reference image for face or style
consistency. This is ideal when you only have 1-2 reference images and don't
want to train a LoRA.
}
\examples{
\dontrun{
# Face consistency with single reference photo
tti_base("http://localhost:8000")
ip_adapter(
  prompt = "A photo of a man as an astronaut on the moon",
  reference_image = "face_photo.jpg",
  ip_adapter_type = "faceid",
  file = "astronaut.png"
)

# Style transfer
ip_adapter(
  prompt = "A cat sitting on a windowsill",
  reference_image = "painting_style.jpg",
  ip_adapter_type = "standard",
  ip_adapter_scale = 0.8,
  file = "styled_cat.png"
)

# Best face quality with FaceID Plus
ip_adapter(
  prompt = "Portrait photo, professional lighting, studio background",
  reference_image = "my_face.jpg",
  ip_adapter_type = "faceid-plus",
  ip_adapter_scale = 0.7,
  file = "portrait.png"
)
}

}
